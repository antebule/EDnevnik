<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ba.sum.fpmoz.abule.pma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EDnevnik">
        <activity android:name=".UserStudentActivity"></activity>
        <activity android:name=".TabbedUserAdminActivity" />
        <activity android:name=".UserAdminActivity" />
        <activity android:name=".ClassViewActivity" />
        <activity
            android:name=".ClassesActivity"
            android:label="@string/title_activity_classes"
            android:theme="@style/Theme.EDnevnik.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>